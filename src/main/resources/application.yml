spring:
  data:
    mongodb:
      database: testcontainers
      uri: mongodb://localhost:27017/testcontainers?readPreference=primary&appname=MongoDB%20Compass&directConnection=true&ssl=false

springdoc:
  swagger-ui:
    disable-swagger-default-url: true
    operations-sorter: method

# http://localhost:8080/v3/api-docs
# http://localhost:8080/v3/api-docs.yaml
# http://localhost:8080/swagger-ui.html

projects:
  url: http://localhost:8081/api/projects

localstack:
  # This way we can distinguish from real aws to localstack
  url: default
  s3:
    url: default