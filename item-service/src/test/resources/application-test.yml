server:
  port: 8082
  servlet:
    context-path: /item-service
    application-display-name: Mylms Items Service
spring:
  application:
    name: item-service
  # H2 DB
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:test
    driverClassName: org.h2.Driver
    username: sa
    password: password
    jpa:
      database-platform: org:hibernate:dialect:H2Dialect

#Actuator info
info:
  app: item-service
  version: 0.1
  description: description